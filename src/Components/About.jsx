//Imported React 
import React from "react";

//Created a class that export only the About react component
export default class About extends React.Component{
    render() {
        return (
            <div>
                About Page
            </div>
        )
    }
}